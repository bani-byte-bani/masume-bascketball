# CLAUDE.md

このリポジトリで作業するときの指示。

## このプロジェクトについて

ミニバスチームのお楽しみ会で遊ぶボードゲーム「マス目バスケ」のWeb実装。
**詳細な仕様は必ず [docs/SPEC.md](docs/SPEC.md) を読んでから作業すること。**

## 絶対に守ること

1. **単一HTMLファイルを維持する。** `masume-basket-game.html` の中にHTML/CSS/JSをすべて含める。
   ビルドツール、npm、外部CDNを導入しない。GitHub Pagesに置くだけで動く状態を保つ。
2. **`localStorage` / `sessionStorage` を使わない。** 状態はJS変数 `S` に持つ。
3. **`confirm()` / `alert()` を使わない。** サンドボックスでブロックされ無反応になる。
4. **UIの文言は小学生向け。** ひらがな多め、専門用語を避ける。

## 作業の進め方

- 修正したら、HTML内の「修正日時」の日付を今日の日付に更新する。
- ルールやバランスを変えたら、`docs/SPEC.md` の該当箇所も一緒に更新する。
- **バランス調整は感覚でやらない。** 盤面を再現した検証スクリプトを `sim/` に置き、
  数千回試行して「シュート到達率」「ノーマーク率」「1攻撃あたり期待得点」を
  比較してから判断する。判断材料になった数値はSPEC.mdの第6章に追記する。

## 確認方法

ブラウザで開くのが基本だが、JSの構文チェックだけなら以下で確認できる。

```bash
node -e "
const h=require('fs').readFileSync('masume-basket-game.html','utf8');
const src=h.match(/<script>([\s\S]*)<\/script>/)[1];
new Function('document','setInterval','confirm', src);
console.log('構文OK');
"
```

配置の重複・盤外チェックなど、ロジック単体の検証は `sim/` にスクリプトを書いて `node` で回す。

## 用語

- **駒 / プレイヤー**： `S.pieces` の各要素。`r`(行)は1が手前、ROWSがゴール側。
- **手番**： 攻撃側の移動 → 行動選択 → 守備側の移動、で1セット。
- **ショットクロック**： 攻撃側の移動回数の上限。`N × 3`（背番号3周ぶん）。
